# Variables d'environnement pour le client Vite (prod)
# A définir dans Netlify > Site Settings > Build & deploy > Environment
# ou dans un fichier .env.production local si besoin.
# Sans slash final.
VITE_SERVER_ORIGIN=https://your-backend.example.com
